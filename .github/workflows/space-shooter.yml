name: Update Space Shooter Game

on:
  schedule:
    - cron: '0 0 * * *'  # Her gün gece yarısı (UTC) otomatik günceller
  workflow_dispatch:     # İstediğin zaman manuel çalıştırmana olanak tanır

permissions:
  contents: write

jobs:
  update-game:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: czl9707/gh-space-shooter@v1
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          output-path: 'game.gif'
          username: 'CENGTOKSOY'
          strategy: 'random'
